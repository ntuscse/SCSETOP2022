import React from "react";

const FireArea = ({ allowedDropEffect }) => {
  return (
    <div
      style={{
        backgroundColor: "black",
        height: "400px",
      }}
    ></div>
  );
};

export default FireArea;
