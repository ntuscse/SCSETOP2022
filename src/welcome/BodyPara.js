import React from "react";

const  Body = (props) => {
    return (
        <div style={{minWidth:'300px', maxWidth:'600px'}}>
            <p>{props.text}</p>
        </div>
    )
    }
    
    export default Body